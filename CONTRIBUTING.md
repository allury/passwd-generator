# 贡献指南

感谢你愿意改进安全密码生成器。

## 开始之前

- 从 `main` 分支创建功能分支。
- 每次提交尽量只解决一个明确问题。
- 安全漏洞请按 [SECURITY.md](SECURITY.md) 私下报告，不要提交公开 Issue。

## 本地验证

项目要求 PHP 8.0 或更高版本。提交前请至少执行：

```bash
php -l passwd.php
```

如需在浏览器中验证，可启动 PHP 内置服务器：

```bash
php -S 127.0.0.1:8080
```

然后访问 `http://127.0.0.1:8080/passwd.php`，检查：

- 不同长度和字符类型组合均能正常生成密码。
- AJAX 交互与无 JavaScript 的表单提交均可使用。
- 复制、主题切换和强度提示工作正常。
- 页面、日志和分析服务不会记录生成的密码或其他敏感信息。

## 代码与文档

- 文件使用 UTF-8 编码和 LF 换行。
- 保持单文件部署特性，新增依赖前请说明必要性。
- 行为变化应同步更新 README 或相关说明。
- 提交信息应简洁说明变更目的。
