## 概要

本版本改进异常输入处理、异步生成失败反馈和前端初始化稳定性。

## 修复与优化

- 增强服务端密码长度和字符选项校验，避免异常请求影响响应格式。
- 统一 AJAX 生成失败的 HTTP 状态和错误字段，便于前端及调用方正确处理。
- 兼容浏览器存储不可用的情况，避免主题初始化异常阻断页面交互。

## 验证

- 通过 PHP 8.0 与 PHP 8.4 语法检查。
- 扩展密码长度、字符选项和异常输入回归测试。

## 部署建议

生产环境仍建议通过 HTTPS 访问。
