## 概要

单文件 PHP 密码生成器，无数据库和外部依赖。

## 新增

- 默认排除易混淆字符（`0O1lI`）。
- 默认避免连续重复字符。

## 优化

- 改进密码生成逻辑与可读性。
- 完善边界情况处理。

## 使用方式

1. 下载本发布中的 `password.php`。
2. 上传至支持 PHP 的网站目录。
3. 通过浏览器直接访问即可使用。

## 环境要求

- PHP 8.0 或更高版本。

## 部署建议

生产环境仍建议通过 HTTPS 访问。
